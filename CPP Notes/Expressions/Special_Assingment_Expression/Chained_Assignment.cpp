#include <iostream>

using  namespace std;
int main() 
{
    int a;
    int b;
    a = b = 80; // Values can't be assinged during declaration
    cout << "Values of 'a' and 'b' are :" << a << " " << b << endl;
    return 0;
}